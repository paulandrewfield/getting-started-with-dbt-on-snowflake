tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: transformer
      database: tasty_bytes_dbt_db_pf
      schema: dev
      warehouse: tasty_bytes_dbt_wh
      threads: 8
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: transformer
      database: tasty_bytes_dbt_db_pf
      schema: prod
      warehouse: tasty_bytes_dbt_wh
      threads: 8
